# Exakt gepinnt. Der taegliche Scrape-Job (scrape.yml) installiert diese Pakete
# und liefert danach webapp/ als GitHub-Pages-Artefakt aus. Ohne Pin liefe bei
# jedem Lauf die NEUESTE PyPI-Fassung - ein kompromittiertes Release koennte so
# Schadcode in die ausgelieferte App bringen. Beim Anheben die Versionen bewusst
# pruefen (Changelog), nicht blind hochsetzen.
requests==2.32.5
beautifulsoup4==4.14.3
